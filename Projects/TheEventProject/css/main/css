@font-face {
  font-family: GothamRoundedBook;
  src: url(../fonts/GothamRnd-Book.otf) format("opentype");
}
@font-face {
  font-family: GothamRoundedBookItalic;
  src: url(../fonts/gothamrounded-bookitalic.otf) format("opentype");
}
@font-face {
  font-family: GothaRoundedMudium;
  src: url(../fonts/gothamrounded-medium.otf) format("opentype");
}
@font-face {
  font-family: GothamRoundedMediumItalic;
  src: url(../fonts/gothamrounded-mediumitalic.otf) format("opentype");
}
/* http://meyerweb.com/eric/tools/css/reset/
   v2.0-modified | 20110126
   License: none (public domain)
*/
html, body, div, span, applet, object, iframe,
h1, h2, h3, h4, h5, h6, p, blockquote, pre,
a, abbr, acronym, address, big, cite, code,
del, dfn, em, img, ins, kbd, q, s, samp,
small, strike, strong, sub, sup, tt, var,
b, u, i, center,
dl, dt, dd, ol, ul, li,
fieldset, form, label, legend,
table, caption, tbody, tfoot, thead, tr, th, td,
article, aside, canvas, details, embed,
figure, figcaption, footer, header, hgroup,
menu, nav, output, ruby, section, summary,
time, mark, audio, video {
  margin: 0;
  padding: 0;
  border: 0;
  font-size: 100%;
  font: inherit;
  vertical-align: baseline;
}

/* make sure to set some focus styles for accessibility */
:focus {
  outline: 0;
}

/* HTML5 display-role reset for older browsers */
article, aside, details, figcaption, figure,
footer, header, hgroup, menu, nav, section {
  display: block;
}

body {
  line-height: 1;
}

ol, ul {
  list-style: none;
}

blockquote, q {
  quotes: none;
}

blockquote:before, blockquote:after,
q:before, q:after {
  content: "";
  content: none;
}

table {
  border-collapse: collapse;
  border-spacing: 0;
}

input[type=search]::-webkit-search-cancel-button,
input[type=search]::-webkit-search-decoration,
input[type=search]::-webkit-search-results-button,
input[type=search]::-webkit-search-results-decoration {
  -webkit-appearance: none;
  -moz-appearance: none;
}

input[type=search] {
  -webkit-appearance: none;
  -moz-appearance: none;
  -webkit-box-sizing: content-box;
  -moz-box-sizing: content-box;
  box-sizing: content-box;
}

textarea {
  overflow: auto;
  vertical-align: top;
  resize: vertical;
}

/**
 * Correct `inline-block` display not defined in IE 6/7/8/9 and Firefox 3.
 */
audio,
canvas,
video {
  display: inline-block;
  *display: inline;
  *zoom: 1;
  max-width: 100%;
}

/**
 * Prevent modern browsers from displaying `audio` without controls.
 * Remove excess height in iOS 5 devices.
 */
audio:not([controls]) {
  display: none;
  height: 0;
}

/**
 * Address styling not present in IE 7/8/9, Firefox 3, and Safari 4.
 * Known issue: no IE 6 support.
 */
[hidden] {
  display: none;
}

/**
 * 1. Correct text resizing oddly in IE 6/7 when body `font-size` is set using
 *    `em` units.
 * 2. Prevent iOS text size adjust after orientation change, without disabling
 *    user zoom.
 */
html {
  font-size: 100%;
  /* 1 */
  -webkit-text-size-adjust: 100%;
  /* 2 */
  -ms-text-size-adjust: 100%;
  /* 2 */
}

/**
 * Address `outline` inconsistency between Chrome and other browsers.
 */
a:focus {
  outline: thin dotted;
}

/**
 * Improve readability when focused and also mouse hovered in all browsers.
 */
a:active,
a:hover {
  outline: 0;
}

/**
 * 1. Remove border when inside `a` element in IE 6/7/8/9 and Firefox 3.
 * 2. Improve image quality when scaled in IE 7.
 */
img {
  border: 0;
  /* 1 */
  -ms-interpolation-mode: bicubic;
  /* 2 */
}

/**
 * Address margin not present in IE 6/7/8/9, Safari 5, and Opera 11.
 */
figure {
  margin: 0;
}

/**
 * Correct margin displayed oddly in IE 6/7.
 */
form {
  margin: 0;
}

/**
 * Define consistent border, margin, and padding.
 */
fieldset {
  border: 1px solid #c0c0c0;
  margin: 0 2px;
  padding: 0.35em 0.625em 0.75em;
}

/**
 * 1. Correct color not being inherited in IE 6/7/8/9.
 * 2. Correct text not wrapping in Firefox 3.
 * 3. Correct alignment displayed oddly in IE 6/7.
 */
legend {
  border: 0;
  /* 1 */
  padding: 0;
  white-space: normal;
  /* 2 */
  *margin-left: -7px;
  /* 3 */
}

/**
 * 1. Correct font size not being inherited in all browsers.
 * 2. Address margins set differently in IE 6/7, Firefox 3+, Safari 5,
 *    and Chrome.
 * 3. Improve appearance and consistency in all browsers.
 */
button,
input,
select,
textarea {
  font-size: 100%;
  /* 1 */
  margin: 0;
  /* 2 */
  vertical-align: baseline;
  /* 3 */
  *vertical-align: middle;
  /* 3 */
}

/**
 * Address Firefox 3+ setting `line-height` on `input` using `!important` in
 * the UA stylesheet.
 */
button,
input {
  line-height: normal;
}

/**
 * Address inconsistent `text-transform` inheritance for `button` and `select`.
 * All other form control elements do not inherit `text-transform` values.
 * Correct `button` style inheritance in Chrome, Safari 5+, and IE 6+.
 * Correct `select` style inheritance in Firefox 4+ and Opera.
 */
button,
select {
  text-transform: none;
}

/**
 * 1. Avoid the WebKit bug in Android 4.0.* where (2) destroys native `audio`
 *    and `video` controls.
 * 2. Correct inability to style clickable `input` types in iOS.
 * 3. Improve usability and consistency of cursor style between image-type
 *    `input` and others.
 * 4. Remove inner spacing in IE 7 without affecting normal text inputs.
 *    Known issue: inner spacing remains in IE 6.
 */
button,
html input[type=button],
input[type=reset],
input[type=submit] {
  -webkit-appearance: button;
  /* 2 */
  cursor: pointer;
  /* 3 */
  *overflow: visible;
  /* 4 */
}

/**
 * Re-set default cursor for disabled elements.
 */
button[disabled],
html input[disabled] {
  cursor: default;
}

/**
 * 1. Address box sizing set to content-box in IE 8/9.
 * 2. Remove excess padding in IE 8/9.
 * 3. Remove excess padding in IE 7.
 *    Known issue: excess padding remains in IE 6.
 */
input[type=checkbox],
input[type=radio] {
  box-sizing: border-box;
  /* 1 */
  padding: 0;
  /* 2 */
  *height: 13px;
  /* 3 */
  *width: 13px;
  /* 3 */
}

/**
 * 1. Address `appearance` set to `searchfield` in Safari 5 and Chrome.
 * 2. Address `box-sizing` set to `border-box` in Safari 5 and Chrome
 *    (include `-moz` to future-proof).
 */
input[type=search] {
  -webkit-appearance: textfield;
  /* 1 */
  -moz-box-sizing: content-box;
  -webkit-box-sizing: content-box;
  /* 2 */
  box-sizing: content-box;
}

/**
 * Remove inner padding and search cancel button in Safari 5 and Chrome
 * on OS X.
 */
input[type=search]::-webkit-search-cancel-button,
input[type=search]::-webkit-search-decoration {
  -webkit-appearance: none;
}

/**
 * Remove inner padding and border in Firefox 3+.
 */
button::-moz-focus-inner,
input::-moz-focus-inner {
  border: 0;
  padding: 0;
}

/**
 * 1. Remove default vertical scrollbar in IE 6/7/8/9.
 * 2. Improve readability and alignment in all browsers.
 */
textarea {
  overflow: auto;
  /* 1 */
  vertical-align: top;
  /* 2 */
}

/**
 * Remove most spacing between table cells.
 */
table {
  border-collapse: collapse;
  border-spacing: 0;
}

html,
button,
input,
select,
textarea {
  color: #222;
}

::-moz-selection {
  background: #b3d4fc;
  text-shadow: none;
}

::selection {
  background: #b3d4fc;
  text-shadow: none;
}

img {
  vertical-align: middle;
}

fieldset {
  border: 0;
  margin: 0;
  padding: 0;
}

textarea {
  resize: vertical;
}

.chromeframe {
  margin: 0.2em 0;
  background: #ccc;
  color: #000;
  padding: 0.2em 0;
}

p {
  font-family: GothaRoundedMudium;
  font-size: 12px;
  color: #ffffff;
  text-align: center;
}

h2, h3 {
  margin-bottom: 15px;
}

h2 {
  font-family: GothaRoundedBook, sans-serif;
  font-size: 30px;
  color: #261158;
  text-align: center;
}

h3 {
  font-family: GothaRoundedMudium;
  font-size: 15px;
  color: #b6b2b2;
  text-align: center;
}

h4 {
  font-family: GothaRoundedMudium;
  font-size: 15px;
  color: #726f6f;
  text-align: center;
}

h5 {
  font-family: GothaRoundedBook, sans-serif;
  font-size: 30px;
  color: #ffffff;
}

a {
  font-family: GothaRoundedBook, sans-serif;
  font-size: 15px;
  color: #abb1b6;
}

.container {
  max-width: 1110px;
  padding: 0px;
}

a:hover {
  text-decoration: none;
}

.divider {
  width: 85px;
  height: 5px;
  margin: 0 auto;
  margin-bottom: 50px;
  border: 1px solid #f82249;
  background-color: #f82249;
}

header {
  background: url(../img/intro-bg.jpg) center/cover no-repeat;
  background-attachment: fixed;
}
header .container {
  width: 100%;
  max-width: 100%;
  background-color: rgba(12, 35, 80, 0.7);
  padding-bottom: 170px;
}
header .navigation {
  display: flex;
  align-items: center;
  justify-content: space-between;
  max-width: 1110px;
  margin: 0 auto;
  margin-bottom: 190px;
}
header img {
  height: 60px;
}
header ul {
  display: flex;
  flex-direction: row;
  align-items: center;
  list-style-type: none;
}
header li {
  margin-right: 15px;
}
header a {
  position: relative;
  padding: 10px 0px;
  font-weight: bold;
}
header a:hover {
  color: #ffffff;
}
header nav .buy-tickets {
  padding: 5px;
  background-color: #f82249;
  border: 2px solid #f82249;
  border-radius: 50px;
  transition: 0.5s;
}
header nav .buy-tickets:hover {
  background-color: transparent;
  transition: 0.5s;
}
header nav .buy-tickets a:hover:before {
  visibility: hidden;
}
header nav a:before {
  content: "";
  position: absolute;
  width: 0;
  height: 2px;
  bottom: 0;
  left: 0;
  background-color: #f82249;
  visibility: hidden;
  transition: all 0.3s ease-in-out 0s;
}
header nav a:hover:before {
  visibility: visible;
  width: 100%;
}
header .intro {
  display: flex;
  flex-direction: column;
  align-items: center;
  max-width: 1110px;
  margin: 0 auto;
  margin-bottom: 230px;
}
header .intro a {
  display: flex;
  flex-direction: column;
}
header h1 {
  font-family: GothamRoundedBook;
  font-size: 40px;
  color: #ffffff;
  margin-bottom: 40px;
}
header span {
  color: #f82249;
}
header p {
  font-family: GothaRoundedMudium;
  font-size: 12px;
  color: #ffffff;
  margin-bottom: 30px;
}
header i {
  margin: 0 auto;
  margin-bottom: 30px;
  z-index: 2;
  color: #f82249;
}
header .spinner-grow {
  width: 80px;
  height: 80px;
  margin-top: -100px;
  color: #abb1b6;
}
header .about-event {
  padding: 10px;
  border: 2px solid #f82249;
  border-radius: 50px;
  transition: 0.5s;
}
header .about-event:hover {
  background-color: #f82249;
}

.about {
  background: url(../img/about-bg.jpg) center/cover no-repeat;
  background-attachment: fixed;
}
.about .container {
  width: 100%;
  max-width: 100%;
  display: flex;
  justify-content: center;
  background-color: rgba(42, 51, 70, 0.7);
}
.about .container-about-event {
  max-width: 30%;
  padding: 45px 0px;
  margin-right: 25px;
}
.about h5 {
  margin-bottom: 10px;
}
.about h3 {
  margin-bottom: 10px;
  text-align: left;
}

.speakers {
  padding: 50px 0px;
}
.speakers .container-about-speakers {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-around;
}
.speakers .speaker1 {
  background: url(../img/speakers/speaker-1.jpg) center/cover no-repeat;
}
.speakers .speaker2 {
  background: url(../img/speakers/speaker-2.jpg) center/cover no-repeat;
}
.speakers .speaker3 {
  background: url(../img/speakers/speaker-3.jpg) center/cover no-repeat;
}
.speakers .speaker4 {
  background: url(../img/speakers/speaker-4.jpg) center/cover no-repeat;
}
.speakers .speaker5 {
  background: url(../img/speakers/speaker-5.jpg) center/cover no-repeat;
}
.speakers .speaker6 {
  background: url(../img/speakers/speaker-6.jpg) center/cover no-repeat;
}
.speakers .speaker1, .speakers .speaker2, .speakers .speaker3, .speakers .speaker4, .speakers .speaker5, .speakers .speaker6 {
  width: 350px;
  height: 265px;
  margin-bottom: 10px;
  display: flex;
  align-items: flex-end;
  justify-content: center;
}
.speakers .speaker1 h3, .speakers .speaker2 h3, .speakers .speaker3 h3, .speakers .speaker4 h3, .speakers .speaker5 h3, .speakers .speaker6 h3 {
  margin-bottom: 5px;
}
.speakers h2, .speakers h3 {
  margin-bottom: 15px;
}
.speakers p {
  text-align: center;
  margin-bottom: 5px;
}
.speakers a {
  color: #ffffff;
  font-size: 30px;
  transition: ease 0.3s;
}
.speakers a:hover {
  color: #f82249;
}
.speakers .details {
  width: 100%;
  height: 50px;
  transition: 0.3s;
  background-color: rgba(12, 35, 80, 0.7);
  overflow: hidden;
}
.speakers .details:hover {
  height: 80px;
}
.speakers .social {
  display: flex;
  justify-content: center;
  padding-bottom: 10px;
  transition: 0.3s;
}
.speakers .social a {
  margin-left: 5px;
  font-size: 15px;
}
.speakers .social a:nth-child(1) {
  margin-left: 0px;
}

.schedule {
  background-color: #f0f3f5b4;
  padding: 50px 0px;
}
.schedule p {
  color: #261158;
}
.schedule .slider-nav-schedule {
  margin-bottom: 25px;
}
.schedule .schedule-info {
  display: flex;
  justify-content: space-around;
  margin-bottom: 15px;
}
.schedule .day-btn {
  display: flex;
  align-items: center;
  width: 200px;
  height: 40px;
  border: 1px solid #261158;
  border-radius: 50px;
  background-color: #261158;
  transition: 0.3s;
}
.schedule .day-btn p {
  margin: 0 auto;
  font-size: 18px;
  color: #ffffff;
}
.schedule .day-btn:hover {
  cursor: pointer;
  background-color: #f82249;
  border-color: #f82249;
}
.schedule .schedule-item {
  display: flex;
}
.schedule .schedule-item h3 {
  color: #261158;
  text-align: left;
}
.schedule .information p {
  color: #4b67a0;
}
.schedule .slider-for-schedule {
  max-width: 710px;
  margin: auto;
}
.schedule .time {
  margin-right: 20px;
}
.schedule img {
  width: 50px;
  height: 50px;
  margin-bottom: 5px;
  border-radius: 50%;
}

.event-venue {
  padding: 50px 0px;
}
.event-venue .container {
  max-width: 100%;
}
.event-venue .venue-about {
  display: flex;
  margin-bottom: 5px;
}
.event-venue .venue-map {
  max-width: 50%;
}
.event-venue .venue-info {
  max-width: 59%;
  background: url(../img/gallery/1.jpg) center/cover no-repeat;
}
.event-venue .venue-info h2 {
  max-width: 60%;
  color: #ffffff;
  font-size: 40px;
  text-align: left;
}
.event-venue .venue-info p {
  max-width: 60%;
  font-size: 20px;
  text-align: left;
}
.event-venue .venue-info-container {
  padding-left: 20px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  height: 100%;
  background-color: rgba(12, 35, 80, 0.7);
}
.event-venue #gallery {
  display: flex;
}
.event-venue #gallery img {
  width: 100%;
  padding: 5px 2px;
  overflow: hidden;
  transition: 0.3s;
}
.event-venue #gallery img:hover {
  transform: scale(1.1);
}
.event-venue #gallery a {
  overflow: hidden;
}

.hotels {
  padding: 50px 0px;
  background-color: #f0f3f5b4;
}
.hotels .hotels-info {
  display: flex;
}
.hotels .card-body {
  display: flex;
  flex-direction: column;
  align-items: center;
}
.hotels a {
  font-size: 25px;
  color: #261158;
  transition: 0.3s;
  margin-bottom: 5px;
}
.hotels a:hover {
  color: #f82249;
}
.hotels p {
  color: #261158;
}
.hotels .star {
  margin-bottom: 5px;
}
.hotels i {
  color: #eeea0c;
}
.hotels .card {
  margin-left: 5px;
  font-size: 15px;
}
.hotels .card:nth-child(1) {
  margin-left: 0px;
}

.gallery {
  padding: 50px 0px;
}
.gallery .container {
  width: 100%;
  max-width: 100%;
}
.gallery img {
  width: 100%;
  height: 100%;
  padding: 5px;
}

.slick-next {
  display: none !important;
}

.slick-prev {
  display: none !important;
}

.sponsors {
  padding: 50px 0px;
  background-color: #f0f3f5b4;
}
.sponsors .sponsors-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
}
.sponsors .sponsors-logo {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 55px 0px;
  width: 25%;
  background-color: #ffffff;
  border: solid 1px #bebebe;
}
.sponsors img {
  transition: 0.3s;
  width: 85%;
}
.sponsors img:hover {
  transform: scale(1.1);
}

.faq {
  padding: 50px 0px;
}
.faq a {
  display: flex;
  justify-content: space-between;
  color: #242424;
}
.faq p {
  padding: 10px;
  color: #abb1b6;
  text-align: left;
}

.tickets {
  padding: 50px 0px;
  background-color: #f0f3f5b4;
}
.tickets .tickets-info {
  display: flex;
  justify-content: center;
}
.tickets .card {
  width: 30%;
  margin-left: 10px;
  box-shadow: -1px 0px 20px 0px #dfdfdf;
}
.tickets .card:nth-child(1) {
  margin-left: 0px;
}
.tickets hr {
  margin-bottom: 30px;
}
.tickets li {
  margin-bottom: 45px;
  display: flex;
}
.tickets i {
  margin-right: 20px;
}
.tickets p {
  color: #161616;
  text-align: left;
}
.tickets .text-muted p {
  color: #726f6f;
}
.tickets .card-link {
  padding: 5px;
  margin: 0 auto;
  display: flex;
  justify-content: center;
  background-color: #f82249;
  border: 2px solid #f82249;
  border-radius: 50px;
  color: #ffffff;
  transition: 0.5s;
}
.tickets .card-link:hover {
  transition: 0.5s;
  background-color: #261158;
  border-color: #261158;
}

.contact {
  padding: 50px 0px;
}
.contact .contact-block {
  display: flex;
  justify-content: space-around;
  margin-bottom: 40px;
}
.contact h5 {
  margin-bottom: 30px;
  color: #261158;
}
.contact p, .contact a {
  color: #0c2350;
}
.contact a {
  transition: 0.3s;
}
.contact a:hover {
  color: #f82249;
}
.contact input, .contact textarea {
  font-family: GothaRoundedMudium;
  font-size: 15px;
  color: #726f6f;
}
.contact button {
  font-family: GothaRoundedMudium;
  font-size: 15px;
  color: #726f6f;
  padding: 10px;
  border: 1px solid #f82249;
  border-radius: 50px;
  background-color: #f82249;
  color: #ffffff;
  transition: 0.3s;
}
.contact button:hover {
  background-color: #261158;
  border-color: #261158;
}

footer {
  background-color: #261158;
}

/*# sourceMappingURL=css.map */
